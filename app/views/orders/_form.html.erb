<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
      <%= f.label 'items' %>
    <%= select_tag 'items',
        options_for_select( @items.map { |i| [i.description,i.id] }, order.items.map { |i| i.id } ), include_blank: true, prompt: 'Escolha uma opção', multiple: true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
